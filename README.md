# PE25

Pour lancer l'application :

1. Installer les dépendances frontend : `npm install`
2. Installer les dépendances backend : `pip install -r server/requirements.txt`
3. Démarrer l'application : `npm run start`

L'interface est alors disponible sur http://localhost:8080
